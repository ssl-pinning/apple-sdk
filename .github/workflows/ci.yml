name: CI

on:
  pull_request:
  push:
    branches:
      - master
    paths:
      - 'App/**/*.swift'
      - 'Sources/**/*.swift'
      - 'Tests/**/*.swift'

jobs:
  test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run tests
        run: swift test
